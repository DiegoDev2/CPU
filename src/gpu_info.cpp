#include "gpu_info.h"

// Este es un lugar reservado ya que la API para acceder a la GPU en macOS es más compleja
void getGpuUsage(float &gpuUsage) {
    // Implementación específica de la GPU va aquí
    // Este es un lugar reservado para la obtención del uso de la GPU
    gpuUsage = 0.0f; // Asigna un valor ficticio para evitar errores
}
